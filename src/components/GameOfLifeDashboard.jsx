import { useState, useMemo, useCallback, useRef, useEffect } from "react";
import {
  PieChart, Pie, Cell, RadarChart, PolarGrid, PolarAngleAxis,
  PolarRadiusAxis, Radar, ResponsiveContainer, Tooltip, Legend,
  BarChart, Bar, XAxis, YAxis, CartesianGrid, ReferenceLine,
} from "recharts";

// ── Student data ──────────────────────────────────────────────
// This file is generated by: npm run build-data
// It reads data/master.csv and writes this JSON.
// Ships with 12 sample students so the app works immediately.
import studentData from "../data/students.json";
const LOADED_STUDENTS = studentData.students;

/* ─── Category Definitions ─── */
const CATEGORIES = [
  { key: "housing", label: "Housing", hint: "Rent, rental insurance, etc.", alloc: { needs: 1, wants: 0, goals: 0 }, suggested: 28 },
  { key: "food", label: "Food", hint: "Groceries + dining out", alloc: { needs: 0.5, wants: 0.5, goals: 0 }, suggested: 12 },
  { key: "wheelOfLife", label: "Wheel of Fortune", hint: "Temporary, not in 50/30/20", alloc: null, suggested: 0 },
  { key: "utilities", label: "Utilities", hint: "Heating, electric, internet, phone", alloc: { needs: 1, wants: 0, goals: 0 }, suggested: 8 },
  { key: "transportation", label: "Transportation", hint: "Car, fuel, transit, rideshare", alloc: { needs: 1, wants: 0, goals: 0 }, suggested: 10 },
  { key: "personalCare", label: "Personal Care", hint: "Fitness, hygiene, clothing, giving", alloc: { needs: 0.5, wants: 0.5, goals: 0 }, suggested: 5 },
  { key: "leisure", label: "Leisure & Entertainment", hint: "Subscriptions, gaming, concerts", alloc: { needs: 0, wants: 1, goals: 0 }, suggested: 8 },
  { key: "savings", label: "Savings & Retirement", hint: "Savings, emergency, retirement", alloc: { needs: 0, wants: 0, goals: 1 }, suggested: 15 },
  { key: "pets", label: "Pets", hint: "Pet costs, pet insurance", alloc: { needs: 0, wants: 1, goals: 0 }, suggested: 2 },
  { key: "debt", label: "Debt Management", hint: "Student loans, credit card", alloc: { needs: 0.5, wants: 0, goals: 0.5 }, suggested: 8 },
  { key: "industry", label: "Industry Costs", hint: "Professional associations, equipment", alloc: { needs: 1, wants: 0, goals: 0 }, suggested: 4 },
];

/* ─── Coaching Prompts ─── */
const COACHING = {
  housing: "Housing is your biggest fixed cost. Would roommates, a different neighborhood, or a smaller space free up budget for other goals?",
  food: "Food spending adds up quickly. What's the split between groceries and dining out? Cooking a few more meals a week can make a big difference.",
  utilities: "Utilities can often be negotiated or reduced. Have you compared internet plans or looked into energy-saving habits?",
  transportation: "Transportation costs vary a lot by lifestyle. Is public transit or biking an option for some trips?",
  personalCare: "Personal care is about balance. Which items here feel essential vs. nice-to-have?",
  leisure: "Entertainment spending is important for wellbeing, but it's also the most flexible category. What matters most to you here?",
  savings: "Great that you're saving! Even small, consistent contributions build over time. Is this split between emergency fund and retirement?",
  pets: "Pets are a real commitment. Have you factored in unexpected vet costs or pet insurance?",
  debt: "Paying more than minimums on debt saves money long-term. Which debts have the highest interest rates?",
  industry: "Industry costs are an investment in your career. Are there student discounts or employer-provided options available?",
};

/* ─── Themes ─── */
const THEMES = {
  dark: {
    name: "Dark", bg: "#0B1120", card: "#111B2E", cardHover: "#162035", border: "#1E2D4A",
    accent: "#D4A843", accentDim: "#8B7230", accentBg: "#D4A84315",
    needs: "#4A90D9", wants: "#E5833A", goals: "#3DBB7D",
    needsBg: "#4A90D920", wantsBg: "#E5833A20", goalsBg: "#3DBB7D20",
    text: "#E2E8F0", textDim: "#8899B0", textMuted: "#4A5C78",
    danger: "#EF4444", success: "#10B981", warning: "#F59E0B",
    inputBg: "#0D1526", inputBorder: "#253552",
    headerBg: "linear-gradient(135deg, #111B2E 0%, #0B1120 100%)",
    overlay: "#0B1120ee",
  },
  light: {
    name: "Champlain", bg: "#EFF3F8", card: "#FFFFFF", cardHover: "#F5F8FC", border: "#D0D9E6",
    accent: "#1B3A5C", accentDim: "#2C5A8C", accentBg: "#1B3A5C10",
    needs: "#2563EB", wants: "#D97706", goals: "#059669",
    needsBg: "#2563EB15", wantsBg: "#D9770615", goalsBg: "#05966915",
    text: "#1E293B", textDim: "#475569", textMuted: "#94A3B8",
    danger: "#DC2626", success: "#16A34A", warning: "#D97706",
    inputBg: "#F8FAFC", inputBorder: "#CBD5E1",
    headerBg: "linear-gradient(135deg, #1B3A5C 0%, #0F2640 100%)",
    overlay: "#EFF3F8ee",
  },
};

const CAT_COLORS = ["#4A90D9","#E5833A","#9B59B6","#3DBB7D","#E74C6F","#F1C40F","#1ABC9C","#E67E22","#3498DB","#8E44AD","#2ECC71"];
const fmt = (n) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", minimumFractionDigits: 0, maximumFractionDigits: 0 }).format(n);
const fmtFull = (n) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", minimumFractionDigits: 2 }).format(n);
const pct = (n) => `${(n * 100).toFixed(1)}%`;

/* ── (Component code continues - identical to the last build) ─────
   The full component code is quite long. For the project scaffold
   the complete file is provided below.
   ───────────────────────────────────────────────────────────────── */

// ─── Sankey, Waterfall, Thermometer, PrintSummary, AggregateDashboard ───
// (These are identical to the artifact version. Keeping the file
//  self-contained so it works as a drop-in.)

function SankeyDiagram({ monthlyNet, wheelOfLife, budgetValues, analysis, t }) {
  const effectiveIncome = monthlyNet + (wheelOfLife || 0);
  if (effectiveIncome <= 0) return <div style={{ color: t.textDim, textAlign: "center", padding: 40, fontSize: 13 }}>Enter a student to see income flow</div>;
  const cats = CATEGORIES.filter(c => c.key !== "wheelOfLife");
  const activeCats = cats.filter(c => (budgetValues[c.key] || 0) > 0);
  if (activeCats.length === 0) return <div style={{ color: t.textDim, textAlign: "center", padding: 40, fontSize: 13 }}>Enter budget values to see income flow</div>;
  const PAD = 20, BAR_W = 70, GAP = 6, width = 560;
  const usableH = Math.max(240, activeCats.length * 28 + 60);
  const height = usableH + PAD * 2;
  const col1 = PAD, col2 = PAD + BAR_W + 80, col3 = width - PAD - BAR_W;
  const totalAllocated = activeCats.reduce((s, c) => s + (budgetValues[c.key] || 0), 0);
  const unallocated = Math.max(0, effectiveIncome - totalAllocated);
  const totalCatGaps = Math.max(0, activeCats.length - 1) * GAP;
  const catDrawH = usableH - totalCatGaps;
  const catBars = []; let catCursorY = PAD;
  for (const c of activeCats) { const val = budgetValues[c.key] || 0; const h = Math.max(8, (val / totalAllocated) * catDrawH); catBars.push({ ...c, val, y: catCursorY, h, color: CAT_COLORS[CATEGORIES.findIndex(x => x.key === c.key)], _rightCursor: 0 }); catCursorY += h + GAP; }
  const nwgDefs = [{ key: "needs", label: "Needs", val: analysis.needsTotal, color: t.needs },{ key: "wants", label: "Wants", val: analysis.wantsTotal, color: t.wants },{ key: "goals", label: "Goals", val: analysis.goalsTotal, color: t.goals }].filter(x => x.val > 0);
  const totalNWG = nwgDefs.reduce((s, n) => s + n.val, 0);
  const nwgGaps = Math.max(0, nwgDefs.length - 1) * GAP; const nwgDrawH = usableH - nwgGaps;
  const nwgBars = []; let nwgCursorY = PAD;
  for (const n of nwgDefs) { const h = Math.max(12, (n.val / totalNWG) * nwgDrawH); nwgBars.push({ ...n, y: nwgCursorY, h, _leftCursor: 0 }); nwgCursorY += h + GAP; }
  const incomeBarTop = catBars[0].y; const lastCat = catBars[catBars.length - 1]; const incomeBarH = lastCat.y + lastCat.h - incomeBarTop;
  let incomeCursorY = incomeBarTop;
  const incomeSlots = catBars.map(cb => { const slotH = (cb.val / totalAllocated) * incomeBarH; const slot = { y: incomeCursorY, h: slotH }; incomeCursorY += slotH; return slot; });
  function ribbon(x1, y1, h1, x2, y2, h2) { const cx1 = x1 + (x2 - x1) * 0.38, cx2 = x1 + (x2 - x1) * 0.62; return `M${x1},${y1} C${cx1},${y1} ${cx2},${y2} ${x2},${y2} L${x2},${y2+h2} C${cx2},${y2+h2} ${cx1},${y1+h1} ${x1},${y1+h1} Z`; }
  const flows1 = catBars.map((cb, i) => ({ d: ribbon(col1+BAR_W, incomeSlots[i].y, incomeSlots[i].h, col2, cb.y, cb.h), color: cb.color }));
  const flows2 = [];
  for (const cb of catBars) { const catDef = CATEGORIES.find(x => x.key === cb.key); if (!catDef?.alloc) continue; for (const nwgBar of nwgBars) { const allocPct = catDef.alloc[nwgBar.key] || 0; if (allocPct === 0) continue; const flowVal = cb.val * allocPct; const srcH = cb.h * allocPct, srcY = cb.y + cb._rightCursor; cb._rightCursor += srcH; const dstH = (flowVal / nwgBar.val) * nwgBar.h, dstY = nwgBar.y + nwgBar._leftCursor; nwgBar._leftCursor += dstH; flows2.push({ d: ribbon(col2+BAR_W, srcY, srcH, col3, dstY, dstH), color: nwgBar.color }); } }
  const inkColor = t.name === "Dark" ? t.bg : "#fff";
  return (
    <svg viewBox={`0 0 ${width} ${height}`} width="100%" style={{ maxHeight: 340 }}>
      <defs><filter id="bG"><feGaussianBlur stdDeviation="2" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs>
      {flows1.map((f,i) => <path key={`a${i}`} d={f.d} fill={f.color} opacity={0.18}/>)}
      {flows2.map((f,i) => <path key={`b${i}`} d={f.d} fill={f.color} opacity={0.18}/>)}
      <rect x={col1} y={incomeBarTop} width={BAR_W} height={incomeBarH} rx={5} fill={t.accent} opacity={0.92} filter="url(#bG)"/>
      <text x={col1+BAR_W/2} y={incomeBarTop+incomeBarH/2-8} textAnchor="middle" fill={inkColor} fontSize={9} fontWeight={700} letterSpacing={1}>MONTHLY</text>
      <text x={col1+BAR_W/2} y={incomeBarTop+incomeBarH/2+8} textAnchor="middle" fill={inkColor} fontSize={12} fontWeight={800}>{fmt(effectiveIncome)}</text>
      {unallocated > 10 && <text x={col1+BAR_W/2} y={incomeBarTop+incomeBarH/2+22} textAnchor="middle" fill={inkColor} fontSize={8} opacity={0.7}>{fmt(unallocated)} left</text>}
      {catBars.map((cb,i) => (<g key={`c${i}`}><rect x={col2} y={cb.y} width={BAR_W} height={cb.h} rx={4} fill={cb.color} opacity={0.88}/>{cb.h>=18 && <text x={col2+BAR_W/2} y={cb.y+cb.h/2+3.5} textAnchor="middle" fill="#fff" fontSize={9} fontWeight={700}>{fmt(cb.val)}</text>}<text x={col2+BAR_W+6} y={cb.y+cb.h/2+3.5} fill={t.textDim} fontSize={9} fontWeight={500}>{cb.label}</text></g>))}
      {nwgBars.map((nb,i) => (<g key={`n${i}`}><rect x={col3} y={nb.y} width={BAR_W} height={nb.h} rx={5} fill={nb.color} opacity={0.92} filter="url(#bG)"/>{nb.h>=26 && <><text x={col3+BAR_W/2} y={nb.y+nb.h/2-5} textAnchor="middle" fill="#fff" fontSize={9} fontWeight={700}>{nb.label}</text><text x={col3+BAR_W/2} y={nb.y+nb.h/2+9} textAnchor="middle" fill="#ffffffcc" fontSize={10} fontWeight={600}>{fmt(nb.val)}</text></>}{nb.h>=14 && nb.h<26 && <text x={col3+BAR_W/2} y={nb.y+nb.h/2+3.5} textAnchor="middle" fill="#fff" fontSize={9} fontWeight={700}>{nb.label}</text>}</g>))}
      <text x={col1+BAR_W/2} y={12} textAnchor="middle" fill={t.textMuted} fontSize={8} fontWeight={600} letterSpacing={1}>INCOME</text>
      <text x={col2+BAR_W/2} y={12} textAnchor="middle" fill={t.textMuted} fontSize={8} fontWeight={600} letterSpacing={1}>CATEGORIES</text>
      <text x={col3+BAR_W/2} y={12} textAnchor="middle" fill={t.textMuted} fontSize={8} fontWeight={600} letterSpacing={1}>50 / 30 / 20</text>
    </svg>
  );
}

function WaterfallChart({ monthlyNet, wheelOfLife, budgetValues, analysis, t }) {
  const effectiveIncome = monthlyNet + (wheelOfLife || 0);
  if (effectiveIncome <= 0) return <div style={{ color: t.textDim, textAlign: "center", padding: 40, fontSize: 13 }}>Enter a student to see budget waterfall</div>;
  const cats = CATEGORIES.filter(c => c.key !== "wheelOfLife"); const activeCats = cats.filter(c => (budgetValues[c.key] || 0) > 0);
  if (activeCats.length === 0) return <div style={{ color: t.textDim, textAlign: "center", padding: 40, fontSize: 13 }}>Enter budget values to see the waterfall</div>;
  const steps = [{ label: "Income", value: effectiveIncome, type: "income", running: effectiveIncome }];
  let running = effectiveIncome;
  for (const c of activeCats) { const val = budgetValues[c.key] || 0; const catDef = CATEGORIES.find(x => x.key === c.key); let nwgType = "needs"; if (catDef?.alloc) { if (catDef.alloc.wants >= catDef.alloc.needs && catDef.alloc.wants >= catDef.alloc.goals) nwgType = "wants"; if (catDef.alloc.goals >= catDef.alloc.needs && catDef.alloc.goals >= catDef.alloc.wants) nwgType = "goals"; if (catDef.alloc.needs === 0.5 && catDef.alloc.wants === 0.5) nwgType = "mixed-nw"; if (catDef.alloc.needs === 0.5 && catDef.alloc.goals === 0.5) nwgType = "mixed-ng"; } running -= val; steps.push({ label: c.label.split(" ")[0], value: -val, type: nwgType, color: CAT_COLORS[CATEGORIES.findIndex(x => x.key === c.key)], running }); }
  steps.push({ label: "Remaining", value: running, type: "remaining", running });
  const PAD_L = 60, PAD_R = 20, PAD_T = 24, PAD_B = 50, barGap = 4, width = 540, height = 300;
  const chartW = width - PAD_L - PAD_R, chartH = height - PAD_T - PAD_B;
  const barW = Math.min(36, (chartW - barGap * (steps.length - 1)) / steps.length);
  const totalBarSpace = barW * steps.length + barGap * (steps.length - 1); const offsetX = PAD_L + (chartW - totalBarSpace) / 2;
  const maxVal = effectiveIncome * 1.05, minVal = Math.min(0, running) * 1.1; const range = maxVal - minVal;
  const yScale = (v) => PAD_T + (1 - (v - minVal) / range) * chartH;
  function getColor(s) { if (s.type === "income") return t.accent; if (s.type === "remaining") return s.value >= 0 ? t.success : t.danger; if (s.type === "needs") return t.needs; if (s.type === "wants") return t.wants; if (s.type === "goals") return t.goals; return "#7A87A8"; }
  const bars = steps.map((step, i) => { const x = offsetX + i * (barW + barGap); if (step.type === "income" || step.type === "remaining") { const top = yScale(Math.max(0, step.value)), bottom = yScale(Math.min(0, step.value)); return { ...step, x, top, bottom, barH: bottom - top }; } const runBefore = step.running - step.value; const top = yScale(runBefore), bottom = yScale(step.running); return { ...step, x, top: Math.min(top, bottom), bottom: Math.max(top, bottom), barH: Math.abs(bottom - top), runBefore }; });
  const tickCount = 5, tickStep = range / tickCount;
  const ticks = Array.from({ length: tickCount + 1 }, (_, i) => { const val = minVal + tickStep * i; return { val, y: yScale(val) }; });
  return (
    <svg viewBox={`0 0 ${width} ${height}`} width="100%" style={{ maxHeight: 320 }}>
      {ticks.map((tk, i) => (<g key={i}><line x1={PAD_L-5} y1={tk.y} x2={width-PAD_R} y2={tk.y} stroke={t.border} strokeWidth={0.5} strokeDasharray={tk.val===0?"none":"3 3"} opacity={tk.val===0?0.6:0.3}/><text x={PAD_L-8} y={tk.y+3} textAnchor="end" fill={t.textMuted} fontSize={9} fontFamily="'JetBrains Mono', monospace">{fmt(tk.val)}</text></g>))}
      {bars.map((bar, i) => { if (i === 0 || bar.type === "remaining") return null; const connY = bar.runBefore !== undefined ? yScale(bar.runBefore) : bars[i-1].top; return <line key={`cn${i}`} x1={bars[i-1].x+barW} y1={connY} x2={bar.x} y2={connY} stroke={t.textMuted} strokeWidth={1} strokeDasharray="2 2" opacity={0.4}/>; })}
      {bars.map((bar, i) => { const color = getColor(bar), isEnd = bar.type === "income" || bar.type === "remaining"; return (<g key={`br${i}`}><rect x={bar.x} y={bar.top} width={barW} height={Math.max(2, bar.barH)} rx={3} fill={color} opacity={isEnd?0.92:0.78}/>{bar.barH > 4 && <text x={bar.x+barW/2} y={isEnd ? bar.top-5 : bar.top+bar.barH/2+3.5} textAnchor="middle" fill={isEnd ? color : "#fff"} fontSize={9} fontWeight={700} fontFamily="'JetBrains Mono', monospace">{bar.value<0?"-":""}{fmt(Math.abs(bar.value))}</text>}<text x={bar.x+barW/2} y={height-PAD_B+14} textAnchor="middle" fill={t.textDim} fontSize={8} fontWeight={500} transform={steps.length>8?`rotate(-35 ${bar.x+barW/2} ${height-PAD_B+14})`:""}>  {bar.label}</text></g>); })}
      {[{l:"Needs",c:t.needs,x:PAD_L},{l:"Wants",c:t.wants,x:PAD_L+60},{l:"Goals",c:t.goals,x:PAD_L+120},{l:"Mixed",c:"#7A87A8",x:PAD_L+180}].map(l=>(<g key={l.l}><rect x={l.x} y={height-12} width={8} height={8} rx={2} fill={l.c} opacity={0.8}/><text x={l.x+12} y={height-4.5} fill={t.textMuted} fontSize={8}>{l.l}</text></g>))}
    </svg>
  );
}

function Thermometer({ allocated, monthlyNet, wheelOfLife, t }) {
  const effectiveIncome = monthlyNet + (wheelOfLife || 0); if (effectiveIncome <= 0) return null;
  const pctAllocated = Math.min(allocated / effectiveIncome, 1.5); const remaining = effectiveIncome - allocated;
  const isOver = remaining < 0, isZero = Math.abs(remaining) < 1;
  const fillColor = isOver ? t.danger : isZero ? t.success : t.accent; const fillHeight = Math.min(pctAllocated, 1.0) * 100;
  const statusText = isOver ? "OVER BUDGET" : isZero ? "ZERO-BASED!" : remaining > 0 ? `${fmt(remaining)} left` : "";
  return (<div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 8, padding: "12px 0" }}><div style={{ fontSize: 11, fontWeight: 700, color: t.textDim, letterSpacing: 1, textTransform: "uppercase" }}>Allocation</div><div style={{ position: "relative", width: 48, height: 180, background: t.inputBg, borderRadius: 24, border: `2px solid ${t.border}`, overflow: "hidden" }}><div style={{ position: "absolute", bottom: 0, left: 0, right: 0, height: `${fillHeight}%`, background: `linear-gradient(to top, ${fillColor}dd, ${fillColor}88)`, borderRadius: "0 0 22px 22px", transition: "height 0.6s cubic-bezier(0.34, 1.56, 0.64, 1), background 0.3s" }}/><div style={{ position: "absolute", bottom: 0, left: 0, right: 0, height: `${fillHeight}%`, minHeight: fillHeight > 5 ? 30 : 0, display: "flex", alignItems: "center", justifyContent: "center" }}>{fillHeight > 10 && <span style={{ color: "#fff", fontSize: 14, fontWeight: 800, textShadow: "0 1px 4px #0008" }}>{(pctAllocated * 100).toFixed(0)}%</span>}</div>{[0.5, 0.8, 1.0].map(v => (<div key={v} style={{ position: "absolute", bottom: `${v * 100}%`, left: 4, right: 4, height: 1, background: v === 1 ? t.success : t.textMuted, opacity: 0.5 }}><span style={{ position: "absolute", right: -28, top: -6, fontSize: 8, color: t.textMuted }}>{(v*100)}%</span></div>))}</div><div style={{ fontSize: 12, fontWeight: 700, color: isOver ? t.danger : isZero ? t.success : t.textDim, textAlign: "center" }}>{statusText}</div><div style={{ fontSize: 10, color: t.textMuted }}>{fmt(allocated)} of {fmt(effectiveIncome)}</div></div>);
}

function PrintSummary({ student, budgetValues, analysis, t, onClose }) {
  if (!student) return null;
  const effectiveIncome = student.monthlyNet + (budgetValues.wheelOfLife || 0);
  const cats = CATEGORIES.filter(c => c.key !== "wheelOfLife" && (budgetValues[c.key] || 0) > 0);
  return (<div style={{ position: "fixed", inset: 0, zIndex: 1000, background: t.overlay, display: "flex", alignItems: "center", justifyContent: "center", padding: 20 }} onClick={onClose}><div onClick={e => e.stopPropagation()} style={{ background: t.card, borderRadius: 16, border: `1px solid ${t.border}`, maxWidth: 600, width: "100%", padding: 32, boxShadow: "0 24px 64px #0004", maxHeight: "90vh", overflowY: "auto" }}><div style={{ textAlign: "center", marginBottom: 20 }}><div style={{ fontSize: 10, fontWeight: 700, color: t.accent, letterSpacing: 2, textTransform: "uppercase" }}>The Game of Life</div><div style={{ fontSize: 22, fontWeight: 800, color: t.text, marginTop: 2 }}>Budget Summary</div></div><div style={{ background: t.inputBg, borderRadius: 10, padding: 16, marginBottom: 16 }}><div style={{ fontSize: 16, fontWeight: 700, color: t.text }}>{student.firstName} {student.lastName}</div><div style={{ fontSize: 12, color: t.textDim, marginTop: 2 }}>{student.programTitle} &middot; {student.section}</div><div style={{ display: "flex", gap: 16, marginTop: 12, flexWrap: "wrap" }}><div><div style={{ fontSize: 9, color: t.textMuted, fontWeight: 600, textTransform: "uppercase" }}>Gross Annual</div><div style={{ fontSize: 14, fontWeight: 700, fontFamily: "'JetBrains Mono', monospace" }}>{fmtFull(student.grossIncome)}</div></div><div><div style={{ fontSize: 9, color: t.textMuted, fontWeight: 600, textTransform: "uppercase" }}>Monthly Net</div><div style={{ fontSize: 14, fontWeight: 700, fontFamily: "'JetBrains Mono', monospace", color: t.accent }}>{fmtFull(student.monthlyNet)}</div></div></div></div><div style={{ fontSize: 12, fontWeight: 700, color: t.text, marginBottom: 8 }}>Monthly Budget Breakdown</div><table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}><thead><tr style={{ borderBottom: `2px solid ${t.border}` }}><th style={{ textAlign: "left", padding: "6px 8px", color: t.textDim, fontWeight: 600 }}>Category</th><th style={{ textAlign: "right", padding: "6px 8px", color: t.textDim, fontWeight: 600 }}>Amount</th><th style={{ textAlign: "right", padding: "6px 8px", color: t.textDim, fontWeight: 600 }}>% of Income</th></tr></thead><tbody>{cats.map(c => { const val = budgetValues[c.key] || 0; return (<tr key={c.key} style={{ borderBottom: `1px solid ${t.border}` }}><td style={{ padding: "6px 8px", color: t.text }}>{c.label}</td><td style={{ padding: "6px 8px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: t.text }}>{fmt(val)}</td><td style={{ padding: "6px 8px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: t.textDim }}>{effectiveIncome > 0 ? pct(val/effectiveIncome) : "---"}</td></tr>); })}</tbody><tfoot><tr style={{ borderTop: `2px solid ${t.border}` }}><td style={{ padding: "6px 8px", fontWeight: 700, color: t.text }}>Total</td><td style={{ padding: "6px 8px", textAlign: "right", fontWeight: 700, fontFamily: "'JetBrains Mono', monospace", color: t.text }}>{fmt(analysis.totalExpenses)}</td><td style={{ padding: "6px 8px", textAlign: "right", fontWeight: 700, fontFamily: "'JetBrains Mono', monospace", color: analysis.remaining >= 0 ? t.success : t.danger }}>{fmt(analysis.remaining)} left</td></tr></tfoot></table><div style={{ display: "flex", gap: 12, marginTop: 16 }}>{[{ label: "Needs", val: analysis.needsPct, target: 0.5, color: t.needs },{ label: "Wants", val: analysis.wantsPct, target: 0.3, color: t.wants },{ label: "Goals", val: analysis.goalsPct, target: 0.2, color: t.goals }].map(n => (<div key={n.label} style={{ flex: 1, background: t.inputBg, borderRadius: 10, padding: 12, textAlign: "center" }}><div style={{ fontSize: 9, fontWeight: 700, color: n.color, letterSpacing: 1, textTransform: "uppercase" }}>{n.label}</div><div style={{ fontSize: 22, fontWeight: 800, fontFamily: "'JetBrains Mono', monospace", color: n.color }}>{pct(n.val)}</div><div style={{ fontSize: 10, color: t.textMuted }}>Target: {pct(n.target)}</div></div>))}</div><div style={{ marginTop: 16, padding: 12, background: t.inputBg, borderRadius: 8, fontSize: 11, color: t.textDim, lineHeight: 1.6 }}>The 50/30/20 rule is a guideline, not a rigid rule. Focus on awareness and intentional choices. Budgeting improves with practice. Questions? personalfinancecoach@champlain.edu</div><div style={{ display: "flex", gap: 8, marginTop: 16 }}><button onClick={() => window.print()} style={{ flex: 1, padding: "10px 16px", background: t.accent, color: t.name === "Dark" ? t.bg : "#fff", border: "none", borderRadius: 8, fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Print</button><button onClick={onClose} style={{ flex: 1, padding: "10px 16px", background: t.inputBg, color: t.textDim, border: `1px solid ${t.border}`, borderRadius: 8, fontSize: 12, fontWeight: 600, cursor: "pointer", fontFamily: "inherit" }}>Close</button></div></div></div>);
}

function AggregateDashboard({ sessionLog, t, fontSize }) {
  if (sessionLog.length === 0) return (<div style={{ textAlign: "center", padding: "60px 20px" }}><div style={{ fontSize: 36, marginBottom: 12, opacity: 0.3 }}>&#128202;</div><div style={{ fontSize: 15 * fontSize, fontWeight: 600, color: t.textDim }}>No students processed yet</div><div style={{ fontSize: 11 * fontSize, color: t.textMuted, marginTop: 4, maxWidth: 360, margin: "8px auto 0" }}>Process students using the "Save &amp; Next" button to build aggregate data here.</div></div>);
  const avgNeeds = sessionLog.reduce((s, e) => s + e.needsPct, 0) / sessionLog.length;
  const avgWants = sessionLog.reduce((s, e) => s + e.wantsPct, 0) / sessionLog.length;
  const avgGoals = sessionLog.reduce((s, e) => s + e.goalsPct, 0) / sessionLog.length;
  const avgRemaining = sessionLog.reduce((s, e) => s + e.remaining, 0) / sessionLog.length;
  const overBudgetCount = sessionLog.filter(e => e.remaining < 0).length;
  const catAvgs = CATEGORIES.filter(c => c.key !== "wheelOfLife").map(c => { const vals = sessionLog.map(e => e.budgetValues[c.key] || 0).filter(v => v > 0); return { name: c.label.split(" ")[0], avg: vals.length > 0 ? vals.reduce((s,v) => s+v, 0) / vals.length : 0, count: vals.length, color: CAT_COLORS[CATEGORIES.findIndex(x => x.key === c.key)] }; }).filter(c => c.count > 0);
  const nwgDist = sessionLog.map(e => ({ needs: e.needsPct * 100, wants: e.wantsPct * 100, goals: e.goalsPct * 100 }));
  return (<div style={{ display: "flex", flexDirection: "column", gap: 16 }}><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(140px, 1fr))", gap: 12 }}>{[{ label: "Students Processed", val: sessionLog.length, color: t.accent },{ label: "Avg Needs %", val: pct(avgNeeds), color: t.needs },{ label: "Avg Wants %", val: pct(avgWants), color: t.wants },{ label: "Avg Goals %", val: pct(avgGoals), color: t.goals },{ label: "Avg Remaining", val: fmt(avgRemaining), color: avgRemaining >= 0 ? t.success : t.danger },{ label: "Over Budget", val: `${overBudgetCount} / ${sessionLog.length}`, color: overBudgetCount > 0 ? t.danger : t.success }].map(s => (<div key={s.label} style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 10, padding: 14, textAlign: "center" }}><div style={{ fontSize: 9 * fontSize, fontWeight: 700, color: t.textMuted, letterSpacing: 0.8, textTransform: "uppercase" }}>{s.label}</div><div style={{ fontSize: 20 * fontSize, fontWeight: 800, color: s.color, fontFamily: "'JetBrains Mono', monospace", marginTop: 4 }}>{s.val}</div></div>))}</div><div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: 16 }}><div style={{ fontSize: 12 * fontSize, fontWeight: 700, color: t.text, marginBottom: 12 }}>Average Category Spending</div><ResponsiveContainer width="100%" height={220}><BarChart data={catAvgs} margin={{ top: 5, right: 20, bottom: 5, left: 20 }}><CartesianGrid strokeDasharray="3 3" stroke={t.border} /><XAxis dataKey="name" tick={{ fill: t.textDim, fontSize: 10 }} /><YAxis tick={{ fill: t.textMuted, fontSize: 10 }} tickFormatter={(v) => `$${v}`} /><Tooltip contentStyle={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 8, fontSize: 12 }} formatter={(v) => fmt(v)} /><Bar dataKey="avg" name="Average $" radius={[4,4,0,0]}>{catAvgs.map((c, i) => <Cell key={i} fill={c.color} opacity={0.85} />)}</Bar></BarChart></ResponsiveContainer></div><div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: 16 }}><div style={{ fontSize: 12 * fontSize, fontWeight: 700, color: t.text, marginBottom: 12 }}>50/30/20 Distribution Across Students</div><ResponsiveContainer width="100%" height={200}><BarChart data={nwgDist} margin={{ top: 5, right: 20, bottom: 5, left: 20 }}><CartesianGrid strokeDasharray="3 3" stroke={t.border} /><XAxis tick={false} /><YAxis tick={{ fill: t.textMuted, fontSize: 10 }} tickFormatter={(v) => `${v}%`} /><Tooltip contentStyle={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 8, fontSize: 12 }} formatter={(v) => `${v.toFixed(1)}%`} /><ReferenceLine y={50} stroke={t.needs} strokeDasharray="4 4" opacity={0.5} /><ReferenceLine y={30} stroke={t.wants} strokeDasharray="4 4" opacity={0.5} /><ReferenceLine y={20} stroke={t.goals} strokeDasharray="4 4" opacity={0.5} /><Bar dataKey="needs" name="Needs %" fill={t.needs} opacity={0.7} stackId="a" /><Bar dataKey="wants" name="Wants %" fill={t.wants} opacity={0.7} stackId="a" /><Bar dataKey="goals" name="Goals %" fill={t.goals} opacity={0.7} stackId="a" /><Legend wrapperStyle={{ fontSize: 11 }} /></BarChart></ResponsiveContainer></div><div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: 16 }}><div style={{ fontSize: 12 * fontSize, fontWeight: 700, color: t.text, marginBottom: 8 }}>Session Log</div><div style={{ overflowX: "auto" }}><table style={{ width: "100%", borderCollapse: "collapse", fontSize: 11 * fontSize }}><thead><tr style={{ borderBottom: `2px solid ${t.border}` }}>{["#","Student","Program","Needs","Wants","Goals","Remaining"].map(h => (<th key={h} style={{ textAlign: h === "Student" || h === "Program" ? "left" : "right", padding: "6px 8px", color: t.textMuted, fontWeight: 600 }}>{h}</th>))}</tr></thead><tbody>{sessionLog.map((e, i) => (<tr key={i} style={{ borderBottom: `1px solid ${t.border}` }}><td style={{ padding: "6px 8px", color: t.textMuted, textAlign: "right" }}>{i + 1}</td><td style={{ padding: "6px 8px", color: t.text, fontWeight: 600 }}>{e.name}</td><td style={{ padding: "6px 8px", color: t.textDim }}>{e.program}</td><td style={{ padding: "6px 8px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: t.needs }}>{pct(e.needsPct)}</td><td style={{ padding: "6px 8px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: t.wants }}>{pct(e.wantsPct)}</td><td style={{ padding: "6px 8px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: t.goals }}>{pct(e.goalsPct)}</td><td style={{ padding: "6px 8px", textAlign: "right", fontFamily: "'JetBrains Mono', monospace", color: e.remaining >= 0 ? t.success : t.danger }}>{fmt(e.remaining)}</td></tr>))}</tbody></table></div></div></div>);
}

/* ── MAIN DASHBOARD (uses LOADED_STUDENTS from JSON import) ── */
export default function GameOfLifeDashboard() {
  const [students] = useState(LOADED_STUDENTS);
  const [search, setSearch] = useState(""); const [selected, setSelected] = useState(null);
  const [budgetValues, setBudgetValues] = useState({}); const [activeViz, setActiveViz] = useState("donut");
  const [flowMode, setFlowMode] = useState("sankey"); const [showResults, setShowResults] = useState(false);
  const [mainView, setMainView] = useState("entry"); const [showPrint, setShowPrint] = useState(false);
  const [whatIfMode, setWhatIfMode] = useState(false); const [sessionLog, setSessionLog] = useState([]);
  const [showSettings, setShowSettings] = useState(false); const [theme, setTheme] = useState("dark");
  const [fontScale, setFontScale] = useState(1); const searchRef = useRef(null); const settingsRef = useRef(null);
  const t = THEMES[theme]; const fs = fontScale;

  useEffect(() => { const handler = (e) => { if (settingsRef.current && !settingsRef.current.contains(e.target)) setShowSettings(false); }; document.addEventListener("mousedown", handler); return () => document.removeEventListener("mousedown", handler); }, []);

  const filtered = useMemo(() => { if (!search.trim()) return []; const q = search.toLowerCase(); return students.filter(s => s.firstName.toLowerCase().includes(q) || s.lastName.toLowerCase().includes(q) || s.studentId.includes(q) || s.email.toLowerCase().includes(q)).slice(0, 8); }, [search, students]);
  const selectStudent = useCallback((s) => { setSelected(s); setSearch(""); setShowResults(false); setBudgetValues({}); setWhatIfMode(false); setMainView("entry"); }, []);
  const updateBudget = useCallback((key, val) => { setBudgetValues(prev => ({ ...prev, [key]: parseFloat(val) || 0 })); }, []);
  const resetForm = useCallback(() => { setSelected(null); setBudgetValues({}); setSearch(""); setWhatIfMode(false); if (searchRef.current) searchRef.current.focus(); }, []);
  const processAndNext = useCallback(() => { if (!selected || analysis.totalExpenses === 0) return; setSessionLog(prev => [...prev, { name: `${selected.firstName} ${selected.lastName}`, program: selected.programTitle, needsPct: analysis.needsPct, wantsPct: analysis.wantsPct, goalsPct: analysis.goalsPct, remaining: analysis.remaining, totalExpenses: analysis.totalExpenses, budgetValues: { ...budgetValues }, timestamp: new Date().toISOString() }]); resetForm(); }, [selected, budgetValues, resetForm]);

  const analysis = useMemo(() => { if (!selected) return { needsTotal: 0, wantsTotal: 0, goalsTotal: 0, totalExpenses: 0, remaining: 0, needsPct: 0, wantsPct: 0, goalsPct: 0, effectiveIncome: 0 }; const wol = budgetValues.wheelOfLife || 0; const eff = selected.monthlyNet + wol; let needs = 0, wants = 0, goals = 0; for (const cat of CATEGORIES) { if (!cat.alloc || cat.key === "wheelOfLife") continue; const val = budgetValues[cat.key] || 0; needs += val * cat.alloc.needs; wants += val * cat.alloc.wants; goals += val * cat.alloc.goals; } const total = needs + wants + goals; return { needsTotal: needs, wantsTotal: wants, goalsTotal: goals, totalExpenses: total, remaining: eff - total, needsPct: eff > 0 ? needs / eff : 0, wantsPct: eff > 0 ? wants / eff : 0, goalsPct: eff > 0 ? goals / eff : 0, effectiveIncome: eff }; }, [selected, budgetValues]);
  const hasData = analysis.totalExpenses > 0;
  const donutData = useMemo(() => { if (!hasData) return [{ name: "Needs", value: 50 }, { name: "Wants", value: 30 }, { name: "Goals", value: 20 }]; return [{ name: "Needs", value: Math.round(analysis.needsPct * 100) || 0 }, { name: "Wants", value: Math.round(analysis.wantsPct * 100) || 0 }, { name: "Goals", value: Math.round(analysis.goalsPct * 100) || 0 }]; }, [hasData, analysis]);
  const targetDonut = [{ name: "Needs", value: 50 }, { name: "Wants", value: 30 }, { name: "Goals", value: 20 }];
  const radarData = useMemo(() => { if (!selected) return []; const eff = analysis.effectiveIncome || 1; return CATEGORIES.filter(c => c.key !== "wheelOfLife").map(c => ({ category: c.label.split(" ")[0], actual: ((budgetValues[c.key] || 0) / eff * 100), suggested: c.suggested })); }, [selected, budgetValues, analysis.effectiveIncome]);
  const DONUT_COLORS = [t.needs, t.wants, t.goals];
  const activePrompts = useMemo(() => { if (!selected || !hasData) return []; const eff = analysis.effectiveIncome || 1; return CATEGORIES.filter(c => c.key !== "wheelOfLife" && (budgetValues[c.key] || 0) > 0).map(c => { const actualPct = ((budgetValues[c.key] || 0) / eff) * 100; const overBy = actualPct - c.suggested; if (overBy > 5) return { key: c.key, label: c.label, actualPct, suggested: c.suggested, overBy, prompt: COACHING[c.key] }; return null; }).filter(Boolean); }, [selected, budgetValues, analysis.effectiveIncome, hasData]);

  return (
    <div style={{ minHeight: "100vh", background: t.bg, color: t.text, fontFamily: "'Outfit', 'Segoe UI', sans-serif", fontSize: 14 * fs }}>
      <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet" />
      <style>{`* { box-sizing: border-box; margin: 0; padding: 0; } input[type=number]::-webkit-inner-spin-button, input[type=number]::-webkit-outer-spin-button { -webkit-appearance: none; margin: 0; } input[type=number] { -moz-appearance: textfield; } input:focus { outline: none; border-color: ${t.accent} !important; box-shadow: 0 0 0 2px ${t.accent}33; } input[type=range] { -webkit-appearance: none; height: 6px; background: ${t.border}; border-radius: 3px; outline: none; } input[type=range]::-webkit-slider-thumb { -webkit-appearance: none; width: 16px; height: 16px; background: ${t.accent}; border-radius: 50%; cursor: pointer; } .viz-tab { cursor: pointer; padding: 6px 14px; border-radius: 8px; font-size: ${11*fs}px; font-weight: 600; letter-spacing: 0.5px; text-transform: uppercase; transition: all 0.2s; border: 1px solid transparent; color: ${t.textDim}; } .viz-tab:hover { background: ${t.cardHover}; } .viz-tab.active { background: ${t.accentBg}; border-color: ${t.accent}55; color: ${t.accent}; } .cat-row { display: grid; grid-template-columns: 1fr 110px; align-items: center; gap: 8px; padding: 8px 12px; border-radius: 8px; transition: background 0.15s; } .cat-row:hover { background: ${t.cardHover}; } .nwg-badge { display: inline-flex; align-items: center; gap: 3px; padding: 1px 6px; border-radius: 4px; font-size: ${8*fs}px; font-weight: 600; letter-spacing: 0.5px; text-transform: uppercase; } @keyframes slideIn { from { opacity: 0; transform: translateY(-8px); } to { opacity: 1; transform: translateY(0); } } .animate-in { animation: slideIn 0.3s ease-out; } .search-result { padding: 10px 14px; cursor: pointer; border-bottom: 1px solid ${t.border}; transition: background 0.1s; } .search-result:hover { background: ${t.accentBg}; } .search-result:last-child { border-bottom: none; } .hdr-btn { background: ${t.inputBg}; border: 1px solid ${t.border}; color: ${t.textDim}; padding: 8px 14px; border-radius: 8px; font-size: ${12*fs}px; font-weight: 600; cursor: pointer; font-family: inherit; transition: all 0.15s; } .hdr-btn:hover { border-color: ${t.accent}; color: ${t.accent}; }`}</style>

      {showPrint && <PrintSummary student={selected} budgetValues={budgetValues} analysis={analysis} t={t} onClose={() => setShowPrint(false)} />}

      {/* Header */}
      <div style={{ background: t.headerBg, borderBottom: `1px solid ${t.border}`, padding: "16px 24px" }}>
        <div style={{ maxWidth: 1200, margin: "0 auto", display: "flex", alignItems: "center", justifyContent: "space-between", flexWrap: "wrap", gap: 12 }}>
          <div><div style={{ fontSize: 10 * fs, fontWeight: 700, color: theme === "dark" ? t.accent : "#D4A843", letterSpacing: 2, textTransform: "uppercase", marginBottom: 2 }}>The Game of Life</div><div style={{ fontSize: 20 * fs, fontWeight: 800, letterSpacing: -0.5, color: theme === "dark" ? t.text : "#fff" }}>First-Year Financial InSight Milestone</div></div>
          <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
            <div style={{ display: "flex", background: theme === "dark" ? t.inputBg : "#0F264044", borderRadius: 8, padding: 2, border: `1px solid ${theme === "dark" ? t.inputBorder : "#ffffff22"}` }}>
              {[{ key: "entry", label: "Budget Entry" }, { key: "aggregate", label: `Aggregate${sessionLog.length > 0 ? ` (${sessionLog.length})` : ""}` }].map(v => (<div key={v.key} onClick={() => setMainView(v.key)} style={{ padding: "6px 12px", borderRadius: 6, fontSize: 11 * fs, fontWeight: 600, cursor: "pointer", transition: "all 0.15s", color: mainView === v.key ? (theme === "dark" ? t.accent : "#D4A843") : (theme === "dark" ? t.textDim : "#ffffffaa"), background: mainView === v.key ? (theme === "dark" ? t.accentBg : "#ffffff18") : "transparent" }}>{v.label}</div>))}
            </div>
            {selected && mainView === "entry" && <><button className="hdr-btn" onClick={() => setShowPrint(true)} style={{ background: theme === "dark" ? t.inputBg : "#0F264044", color: theme === "dark" ? t.textDim : "#ffffffcc", borderColor: theme === "dark" ? t.border : "#ffffff33" }}>Summary</button><button className="hdr-btn" onClick={processAndNext} style={{ background: t.accent, color: theme === "dark" ? t.bg : "#fff", borderColor: t.accent }}>Save &amp; Next</button><button className="hdr-btn" onClick={resetForm} style={{ background: theme === "dark" ? t.inputBg : "#0F264044", color: theme === "dark" ? t.textDim : "#ffffffcc", borderColor: theme === "dark" ? t.border : "#ffffff33" }}>Reset</button></>}
            <div ref={settingsRef} style={{ position: "relative" }}><button className="hdr-btn" onClick={() => setShowSettings(p => !p)} style={{ padding: "8px 10px", background: theme === "dark" ? t.inputBg : "#0F264044", color: theme === "dark" ? t.textDim : "#ffffffcc", borderColor: theme === "dark" ? t.border : "#ffffff33" }}>&#9881;</button>{showSettings && (<div style={{ position: "absolute", top: "calc(100% + 6px)", right: 0, width: 240, background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: 16, zIndex: 200, boxShadow: "0 16px 48px #0004" }}><div style={{ fontSize: 12 * fs, fontWeight: 700, color: t.text, marginBottom: 12 }}>Settings</div><div style={{ marginBottom: 14 }}><div style={{ fontSize: 10 * fs, fontWeight: 600, color: t.textMuted, letterSpacing: 0.5, textTransform: "uppercase", marginBottom: 6 }}>Theme</div><div style={{ display: "flex", gap: 6 }}>{[{ key: "dark", label: "Dark" }, { key: "light", label: "Champlain" }].map(o => (<div key={o.key} onClick={() => setTheme(o.key)} style={{ flex: 1, padding: "8px 12px", borderRadius: 8, textAlign: "center", cursor: "pointer", fontSize: 11 * fs, fontWeight: 600, transition: "all 0.15s", background: theme === o.key ? t.accentBg : t.inputBg, border: `1px solid ${theme === o.key ? t.accent : t.inputBorder}`, color: theme === o.key ? t.accent : t.textDim }}>{o.label}</div>))}</div></div><div><div style={{ fontSize: 10 * fs, fontWeight: 600, color: t.textMuted, letterSpacing: 0.5, textTransform: "uppercase", marginBottom: 6 }}>Font Size: {Math.round(fontScale * 100)}%</div><input type="range" min={0.8} max={1.3} step={0.05} value={fontScale} onChange={e => setFontScale(parseFloat(e.target.value))} style={{ width: "100%" }} /><div style={{ display: "flex", justifyContent: "space-between", fontSize: 9, color: t.textMuted, marginTop: 2 }}><span>Smaller</span><span>Larger</span></div></div></div>)}</div>
            {mainView === "entry" && (<div style={{ position: "relative", width: 280 }}><input ref={searchRef} type="text" value={search} onChange={e => { setSearch(e.target.value); setShowResults(true); }} onFocus={() => setShowResults(true)} onBlur={() => setTimeout(() => setShowResults(false), 200)} placeholder="Search name, ID, email..." style={{ width: "100%", padding: "10px 14px", background: theme === "dark" ? t.inputBg : "#0F264066", border: `1px solid ${theme === "dark" ? t.inputBorder : "#ffffff33"}`, borderRadius: 10, color: theme === "dark" ? t.text : "#fff", fontSize: 13 * fs, fontFamily: "inherit" }} />{showResults && filtered.length > 0 && (<div style={{ position: "absolute", top: "calc(100% + 4px)", left: 0, right: 0, background: t.card, border: `1px solid ${t.border}`, borderRadius: 10, zIndex: 100, maxHeight: 320, overflowY: "auto", boxShadow: "0 16px 48px #0008" }}>{filtered.map(s => (<div key={s.studentId} className="search-result" onMouseDown={() => selectStudent(s)}><div style={{ fontWeight: 600, fontSize: 13 * fs, color: t.text }}>{s.firstName} {s.lastName}</div><div style={{ fontSize: 11 * fs, color: t.textDim, marginTop: 1 }}>{s.programTitle} &middot; {s.section}</div></div>))}</div>)}</div>)}
          </div>
        </div>
      </div>

      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "20px 24px" }}>
        {mainView === "aggregate" && <AggregateDashboard sessionLog={sessionLog} t={t} fontSize={fs} />}
        {mainView === "entry" && <>
          {!selected && (<div style={{ textAlign: "center", padding: "80px 20px" }}><div style={{ fontSize: 48, marginBottom: 16, opacity: 0.3 }}>&#128176;</div><div style={{ fontSize: 18 * fs, fontWeight: 600, color: t.textDim }}>Search for a student to begin</div><div style={{ fontSize: 13 * fs, color: t.textMuted, maxWidth: 400, margin: "8px auto 0" }}>Use the search bar to find a student by name, ID, or email.</div><div style={{ marginTop: 24, fontSize: 11 * fs, color: t.textMuted }}>{students.length} students loaded{sessionLog.length > 0 ? ` · ${sessionLog.length} processed` : ""}</div></div>)}
          {selected && (<div className="animate-in">
            <div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: "16px 20px", marginBottom: 16, display: "flex", flexWrap: "wrap", gap: 20, alignItems: "center" }}><div style={{ flex: 1, minWidth: 200 }}><div style={{ fontSize: 18 * fs, fontWeight: 700 }}>{selected.firstName} {selected.lastName}</div><div style={{ fontSize: 12 * fs, color: t.textDim, marginTop: 2 }}>{selected.programTitle} &middot; {selected.section}</div><div style={{ fontSize: 11 * fs, color: t.textMuted, marginTop: 1 }}>{selected.email}</div></div><div style={{ display: "flex", gap: 20, flexWrap: "wrap" }}>{[{ label: "Gross Annual", val: fmtFull(selected.grossIncome) },{ label: "Benefits", val: fmtFull(selected.benefitsPremium) },{ label: "Fed Tax", val: fmtFull(selected.federalTax) },{ label: "State Tax", val: fmtFull(selected.stateTax) },{ label: "Monthly Net", val: fmtFull(selected.monthlyNet), highlight: true }].map((item, i) => (<div key={i} style={{ textAlign: "center", minWidth: 80 }}><div style={{ fontSize: 9 * fs, fontWeight: 600, color: t.textMuted, letterSpacing: 0.8, textTransform: "uppercase", marginBottom: 3 }}>{item.label}</div><div style={{ fontSize: 15 * fs, fontWeight: 700, fontFamily: "'JetBrains Mono', monospace", color: item.highlight ? t.accent : t.text }}>{item.val}</div></div>))}</div></div>
            <div style={{ display: "grid", gridTemplateColumns: "380px 1fr", gap: 16, alignItems: "start" }}>
              {/* Budget entry */}
              <div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, overflow: "hidden" }}>
                <div style={{ padding: "14px 16px 10px", borderBottom: `1px solid ${t.border}`, display: "flex", justifyContent: "space-between", alignItems: "center" }}><div><div style={{ fontSize: 13 * fs, fontWeight: 700 }}>Budget Categories</div><div style={{ fontSize: 10 * fs, color: t.textMuted, marginTop: 2 }}>{whatIfMode ? "Drag sliders to explore scenarios" : "Enter monthly amounts"}</div></div>{hasData && (<div onClick={() => setWhatIfMode(p => !p)} style={{ padding: "4px 10px", borderRadius: 6, fontSize: 10 * fs, fontWeight: 600, cursor: "pointer", color: whatIfMode ? t.accent : t.textMuted, background: whatIfMode ? t.accentBg : t.inputBg, border: `1px solid ${whatIfMode ? t.accent + "55" : t.inputBorder}` }}>{whatIfMode ? "What If: ON" : "What If"}</div>)}</div>
                <div style={{ padding: "4px 4px 8px" }}>{CATEGORIES.map((cat, i) => { const isWheel = cat.key === "wheelOfLife"; const val = budgetValues[cat.key] || 0; const valStr = budgetValues[cat.key] !== undefined && budgetValues[cat.key] !== 0 ? String(budgetValues[cat.key]) : ""; const allocTags = cat.alloc ? Object.entries(cat.alloc).filter(([,v]) => v > 0).map(([k, v]) => ({ label: k.charAt(0).toUpperCase() + k.slice(1), pct: v, color: k === "needs" ? t.needs : k === "wants" ? t.wants : t.goals, bg: k === "needs" ? t.needsBg : k === "wants" ? t.wantsBg : t.goalsBg })) : []; const maxSlider = Math.max(val * 2, selected.monthlyNet * 0.4); return (<div key={cat.key}><div className="cat-row" style={{ background: isWheel ? t.accentBg : undefined, gridTemplateColumns: whatIfMode && !isWheel ? "1fr" : "1fr 110px" }}><div><div style={{ display: "flex", alignItems: "center", gap: 6, flexWrap: "wrap" }}><div style={{ width: 4, height: 4, borderRadius: 2, background: CAT_COLORS[i], flexShrink: 0 }} /><span style={{ fontSize: 12 * fs, fontWeight: 600, color: isWheel ? t.accent : t.text }}>{cat.label}</span>{allocTags.map(tag => (<span key={tag.label} className="nwg-badge" style={{ color: tag.color, background: tag.bg }}>{tag.pct < 1 ? `${tag.pct*100}%` : ""} {tag.label}</span>))}{isWheel && <span className="nwg-badge" style={{ color: t.accent, background: t.accentBg }}>temporary</span>}</div><div style={{ fontSize: 9 * fs, color: t.textMuted, marginTop: 1, paddingLeft: 10 }}>{cat.hint}</div>{whatIfMode && !isWheel && val > 0 && (<div style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 6, paddingLeft: 10 }}><input type="range" min={0} max={maxSlider} step={5} value={val} onChange={e => updateBudget(cat.key, e.target.value)} style={{ flex: 1 }} /><span style={{ fontSize: 11 * fs, fontWeight: 700, fontFamily: "'JetBrains Mono', monospace", color: t.text, minWidth: 55, textAlign: "right" }}>{fmt(val)}</span></div>)}</div>{(!whatIfMode || isWheel) && (<div style={{ position: "relative" }}><span style={{ position: "absolute", left: 10, top: "50%", transform: "translateY(-50%)", color: t.textMuted, fontSize: 13 * fs, fontFamily: "'JetBrains Mono', monospace", pointerEvents: "none" }}>$</span><input type="number" value={valStr} onChange={e => updateBudget(cat.key, e.target.value)} placeholder="0" min="0" step="5" style={{ width: "100%", padding: "8px 10px 8px 24px", background: t.inputBg, border: `1px solid ${isWheel ? t.accent + "44" : t.inputBorder}`, borderRadius: 8, color: t.text, fontSize: 13 * fs, fontFamily: "'JetBrains Mono', monospace", textAlign: "right" }} /></div>)}</div></div>); })}</div>
                {activePrompts.length > 0 && (<div style={{ padding: "0 8px 8px" }}>{activePrompts.map(p => (<div key={p.key} style={{ padding: "8px 12px", margin: "0 0 4px", background: t.warning + "0a", border: `1px solid ${t.warning}22`, borderRadius: 8 }}><div style={{ fontSize: 10 * fs, fontWeight: 700, color: t.warning, marginBottom: 2 }}>{p.label}: {p.actualPct.toFixed(0)}% of income (suggested: {p.suggested}%)</div><div style={{ fontSize: 11 * fs, color: t.textDim, lineHeight: 1.5 }}>{p.prompt}</div></div>))}</div>)}
                <div style={{ padding: "12px 16px", borderTop: `1px solid ${t.border}`, background: t.bg + (theme === "dark" ? "88" : "44") }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}><span style={{ fontSize: 11 * fs, fontWeight: 600, color: t.textDim }}>Total Expenses</span><span style={{ fontSize: 15 * fs, fontWeight: 700, fontFamily: "'JetBrains Mono', monospace" }}>{fmt(analysis.totalExpenses)}</span></div><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}><span style={{ fontSize: 11 * fs, fontWeight: 600, color: t.textDim }}>Remaining</span><span style={{ fontSize: 15 * fs, fontWeight: 700, fontFamily: "'JetBrains Mono', monospace", color: analysis.remaining < 0 ? t.danger : analysis.remaining < 1 && analysis.totalExpenses > 0 ? t.success : t.accent }}>{analysis.remaining < 0 ? "-" : ""}{fmt(Math.abs(analysis.remaining))}</span></div><div style={{ display: "flex", gap: 8, marginTop: 10 }}>{[{ label: "Needs", val: analysis.needsPct, color: t.needs, target: 0.5 },{ label: "Wants", val: analysis.wantsPct, color: t.wants, target: 0.3 },{ label: "Goals", val: analysis.goalsPct, color: t.goals, target: 0.2 }].map(n => (<div key={n.label} style={{ flex: 1, background: t.inputBg, borderRadius: 8, padding: "8px 10px", textAlign: "center" }}><div style={{ fontSize: 8 * fs, fontWeight: 700, color: n.color, letterSpacing: 1, textTransform: "uppercase" }}>{n.label}</div><div style={{ fontSize: 16 * fs, fontWeight: 800, fontFamily: "'JetBrains Mono', monospace", color: n.color, marginTop: 2 }}>{hasData ? pct(n.val) : "---"}</div><div style={{ fontSize: 9 * fs, color: t.textMuted, marginTop: 1 }}>Target: {pct(n.target)}</div></div>))}</div></div>
              </div>
              {/* Visualizations */}
              <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
                <div style={{ display: "flex", gap: 6, background: t.card, borderRadius: 10, padding: 4, border: `1px solid ${t.border}` }}>{[{ key: "donut", label: "50/30/20 Donut" }, { key: "sankey", label: "Income Flow" }, { key: "radar", label: "Radar" }].map(tab => (<div key={tab.key} className={`viz-tab ${activeViz === tab.key ? "active" : ""}`} onClick={() => setActiveViz(tab.key)}>{tab.label}</div>))}</div>
                <div style={{ display: "grid", gridTemplateColumns: "1fr 100px", gap: 16 }}>
                  <div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: 16, minHeight: 320 }}>
                    {activeViz === "donut" && (<div><div style={{ fontSize: 12 * fs, fontWeight: 700, marginBottom: 4 }}>50/30/20 Comparison</div><div style={{ fontSize: 10 * fs, color: t.textMuted, marginBottom: 12 }}>{hasData ? "Your allocation vs. the recommended target" : "Target shown. Enter values to compare."}</div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }}><div style={{ textAlign: "center" }}><div style={{ fontSize: 10 * fs, fontWeight: 600, color: t.textDim, marginBottom: 4, letterSpacing: 0.5 }}>{hasData ? "YOUR BUDGET" : "TARGET"}</div><ResponsiveContainer width="100%" height={200}><PieChart><Pie data={donutData} cx="50%" cy="50%" innerRadius={55} outerRadius={80} paddingAngle={3} dataKey="value" animationDuration={800}>{donutData.map((_, i) => <Cell key={i} fill={DONUT_COLORS[i]} />)}</Pie><Tooltip formatter={(v) => `${v}%`} contentStyle={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 8, fontSize: 12 }} /></PieChart></ResponsiveContainer>{hasData && <div style={{ display: "flex", justifyContent: "center", gap: 12, marginTop: 4 }}>{donutData.map((d, i) => <span key={i} style={{ fontSize: 10 * fs, color: DONUT_COLORS[i], fontWeight: 600 }}>{d.name}: {d.value}%</span>)}</div>}</div><div style={{ textAlign: "center" }}><div style={{ fontSize: 10 * fs, fontWeight: 600, color: t.textDim, marginBottom: 4, letterSpacing: 0.5 }}>{hasData ? "TARGET" : "IDEAL"}</div><ResponsiveContainer width="100%" height={200}><PieChart><Pie data={targetDonut} cx="50%" cy="50%" innerRadius={55} outerRadius={80} paddingAngle={3} dataKey="value" animationDuration={800}>{targetDonut.map((_, i) => <Cell key={i} fill={DONUT_COLORS[i]} opacity={hasData ? 0.4 : 0.9} />)}</Pie><Tooltip formatter={(v) => `${v}%`} contentStyle={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 8, fontSize: 12 }} /></PieChart></ResponsiveContainer><div style={{ display: "flex", justifyContent: "center", gap: 12, marginTop: 4 }}>{targetDonut.map((d, i) => <span key={i} style={{ fontSize: 10 * fs, color: DONUT_COLORS[i], fontWeight: 600, opacity: hasData ? 0.5 : 1 }}>{d.name}: {d.value}%</span>)}</div></div></div>{hasData && (<div style={{ marginTop: 16, padding: "12px 16px", background: t.inputBg, borderRadius: 10 }}><div style={{ fontSize: 10 * fs, fontWeight: 600, color: t.textDim, marginBottom: 8, letterSpacing: 0.5 }}>DIFFERENCE FROM TARGET</div>{[{ label: "Needs", actual: analysis.needsPct, target: 0.5, color: t.needs },{ label: "Wants", actual: analysis.wantsPct, target: 0.3, color: t.wants },{ label: "Goals", actual: analysis.goalsPct, target: 0.2, color: t.goals }].map(d => { const diff = d.actual - d.target; return (<div key={d.label} style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 6 }}><span style={{ width: 50, fontSize: 11 * fs, fontWeight: 600, color: d.color }}>{d.label}</span><div style={{ flex: 1, height: 6, background: t.border, borderRadius: 3, position: "relative", overflow: "hidden" }}><div style={{ position: "absolute", left: "50%", top: 0, bottom: 0, width: `${Math.min(Math.abs(diff) * 100, 50)}%`, marginLeft: diff < 0 ? `-${Math.min(Math.abs(diff) * 100, 50)}%` : 0, background: d.color, borderRadius: 3, transition: "width 0.5s" }} /><div style={{ position: "absolute", left: "50%", top: -2, bottom: -2, width: 2, background: t.textMuted }} /></div><span style={{ width: 55, fontSize: 11 * fs, fontWeight: 600, fontFamily: "'JetBrains Mono', monospace", textAlign: "right", color: diff > 0.02 ? t.danger : diff < -0.02 ? t.warning : t.success }}>{diff > 0 ? "+" : ""}{(diff * 100).toFixed(1)}%</span></div>); })}</div>)}</div>)}
                    {activeViz === "sankey" && (<div><div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}><div><div style={{ fontSize: 12 * fs, fontWeight: 700 }}>Income Flow</div><div style={{ fontSize: 10 * fs, color: t.textMuted, marginTop: 1 }}>{flowMode === "sankey" ? "Income flowing through categories into needs, wants, and goals" : "Step-by-step cascade through each category"}</div></div><div style={{ display: "flex", background: t.inputBg, borderRadius: 7, padding: 2, border: `1px solid ${t.inputBorder}`, flexShrink: 0 }}>{[{ key: "sankey", label: "Flow" }, { key: "waterfall", label: "Waterfall" }].map(m => (<div key={m.key} onClick={() => setFlowMode(m.key)} style={{ padding: "4px 10px", borderRadius: 5, fontSize: 10 * fs, fontWeight: 600, cursor: "pointer", letterSpacing: 0.3, color: flowMode === m.key ? t.accent : t.textMuted, background: flowMode === m.key ? t.accentBg : "transparent" }}>{m.label}</div>))}</div></div>{flowMode === "sankey" ? <SankeyDiagram monthlyNet={selected.monthlyNet} wheelOfLife={budgetValues.wheelOfLife || 0} budgetValues={budgetValues} analysis={analysis} t={t} /> : <WaterfallChart monthlyNet={selected.monthlyNet} wheelOfLife={budgetValues.wheelOfLife || 0} budgetValues={budgetValues} analysis={analysis} t={t} />}</div>)}
                    {activeViz === "radar" && (<div><div style={{ fontSize: 12 * fs, fontWeight: 700, marginBottom: 4 }}>Category Radar</div><div style={{ fontSize: 10 * fs, color: t.textMuted, marginBottom: 8 }}>Your spending vs. suggested baseline (% of income)</div>{hasData ? (<ResponsiveContainer width="100%" height={300}><RadarChart data={radarData} outerRadius={100}><PolarGrid stroke={t.border} /><PolarAngleAxis dataKey="category" tick={{ fill: t.textDim, fontSize: 10 }} /><PolarRadiusAxis tick={{ fill: t.textMuted, fontSize: 9 }} /><Radar name="Suggested" dataKey="suggested" stroke={t.textMuted} fill={t.textMuted} fillOpacity={0.15} strokeWidth={1} strokeDasharray="4 4" /><Radar name="Your Budget" dataKey="actual" stroke={t.accent} fill={t.accent} fillOpacity={0.25} strokeWidth={2} /><Legend wrapperStyle={{ fontSize: 11 }} /><Tooltip contentStyle={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 8, fontSize: 12 }} formatter={(v) => `${v.toFixed(1)}%`} /></RadarChart></ResponsiveContainer>) : (<div style={{ textAlign: "center", padding: 60, color: t.textMuted, fontSize: 12 * fs }}>Enter budget values to see the radar</div>)}</div>)}
                  </div>
                  <div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: "8px 8px" }}><Thermometer allocated={analysis.totalExpenses} monthlyNet={selected.monthlyNet} wheelOfLife={budgetValues.wheelOfLife || 0} t={t} /></div>
                </div>
                <div style={{ background: t.card, border: `1px solid ${t.border}`, borderRadius: 12, padding: "16px 20px" }}><div style={{ fontSize: 12 * fs, fontWeight: 700, marginBottom: 8 }}>Understanding Your Results</div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 16 }}>{[{ label: "50% NEEDS", color: t.needs, desc: "Housing, utilities, groceries, transportation, insurance, and essential costs." },{ label: "30% WANTS", color: t.wants, desc: "Entertainment, dining out, hobbies, pets, subscriptions, and lifestyle choices." },{ label: "20% GOALS", color: t.goals, desc: "Savings, emergency fund, investments, retirement, and extra debt payments." }].map(n => (<div key={n.label}><div style={{ fontSize: 10 * fs, fontWeight: 700, color: n.color, letterSpacing: 0.5, marginBottom: 4 }}>{n.label}</div><div style={{ fontSize: 11 * fs, color: t.textDim, lineHeight: 1.5 }}>{n.desc}</div></div>))}</div><div style={{ marginTop: 12, padding: "10px 14px", background: t.accentBg, borderRadius: 8, border: `1px solid ${t.accent}22` }}><div style={{ fontSize: 10 * fs, fontWeight: 700, color: t.accent, marginBottom: 3 }}>Wheel of Fortune</div><div style={{ fontSize: 11 * fs, color: t.textDim, lineHeight: 1.5 }}>Represents unexpected life events that temporarily affect your budget. Not counted toward 50/30/20.</div></div><div style={{ marginTop: 8, fontSize: 10 * fs, color: t.textMuted, textAlign: "center" }}>Questions? personalfinancecoach@champlain.edu</div></div>
              </div>
            </div>
          </div>)}
        </>}
      </div>
    </div>
  );
}
