# /data directory

Place your `master.csv` file here.

This is a CSV export of the "Master" sheet from the Master Money Manager workbook.

**This directory is gitignored** because the CSV contains student PII.
